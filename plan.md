# 🗺️ Дорожная карта: Solo Leveling System (Ultimate Edition)

**Видение продукта:** Платформа геймификации с адаптивным ИИ-ядром («Системой»). Приложение превращает рутину в RPG-приключение, где ИИ выступает в роли Гейм-мастера. Пользователь не просто взаимодействует с инструментом — он **живёт** в системе, которая мотивирует через прогресс, историю и социальное признание.

**Легенда статусов:**
- `[x]` — реализовано (базовый функционал работает).
- `[~]` — есть зачатки / частичная реализация; нужна доработка.
- `[ ]` — запланировано к реализации.

---

## ⚙️ Фаза 0: Фундамент, Архитектура и Безопасность
*База проекта, стек, модели, надежность и автоматизация процессов.*

- [x] Стек: Flutter + Riverpod (`ProviderScope`, `StateNotifierProvider`).
- [x] Локальное хранилище: Hive (`DatabaseService`), инициализация в `main.dart`.
- [x] Модели: `Hunter`, `Quest`, `Item`, `Skill`, `Stats`, `Buff`.
- [x] Структура кода: feature-first под `lib/src/`.
- [x] Локализация: JSON-файлы, переключение языка.
- [x] **Безопасность API:** `flutter_secure_storage` для ключей ИИ (миграция из `SharedPreferences` при первом запросе, см. `AIService`).
- [~] **Фоновые задачи:** Локальные напоминания о дедлайнах через `flutter_local_notifications` (пересборка при изменении квестов). Полноценный `workmanager` для штрафов без UI — дальше.
- [~] **Атомарность (Транзакции):** Guard от гонок + единый раннер мутаций (`_runQuestMutation`) и UI-блокировка кнопок во время `completeQuest`/`failQuest`; добавлен snapshot/rollback (`_runQuestMutationAtomic`) для цепочки наград. Полноценные транзакционные блоки на уровне всех операций — дальше.
- [~] **Обеспечение качества:** Unit-тесты: кривая опыта, RNG-дроп (`loot_drop_logic`), агрегат теплокарты (`daily_quest_heatmap`) и rollback псевдо-транзакции квестов (`quest_atomic_rollback_test`); дальше — widget-тесты экранов.
- [x] **CI/CD:** GitHub Actions — анализ + тесты (`.github/workflows/flutter_ci.yml`; сборка APK/AppBundle по желанию добавить job’ом).

---

## ⚔️ Фаза 1: Базовые RPG-механики (MVP) и Аналитика
*Оффлайн-ядро: профиль, квесты, прогресс и визуализация достижений.*

### 1. Профиль Охотника и Аналитика
- [x] UI: экран профиля — уровень, опыт, статы, распределение очков.
- [x] Динамическая кривая опыта: экспоненциальный порог `100 × 1.27^(level-1)` (`ExperienceCurve`), миграция старых сейвов и импорта из линейной формулы.
- [x] Распределение свободных очков (Сила, Ловкость, Интеллект, Живучесть).
- [x] **Аналитика (`fl_chart`):** Радар статов (нормализация к максимуму) и годовая тепловая карта завершённых **ежедневных** квестов (`ProfileAnalyticsSection`, см. профиль).

### 2. Система Квестов и Штрафов
- [x] Модель `Quest` (типы, статусы, теги, сроки).
- [x] UI: списки активных и завершенных квестов.
- [x] Логика начисления базовых наград (EXP, Gold, Stat Points).
- [x] Базовые штрафы за провал ежедневных/недельных квестов.
- [x] **Penalty Zone (Штрафная Зона):** Провал **обязательного** (`mandatory`) квеста → дебафф `penalty_zone` (−50% к множителю опыта, 48 ч) + активный квест `QuestType.penalty`. Выполнение штрафного квеста снимает дебафф. UI: баннер на профиле, тип «Штрафной» на экране квестов.

---

## 🎒 Фаза 2: Экономика, Инвентарь и Навыки
*Лут, магазин и способности. Динамический баланс экономики.*

### 1. Магазин и Экономика
- [x] UI магазина и покупка за золото.
- [~] **Баланс экономики:** `EconomyScale` — золото с квеста и с дропа; **покупка и продажа в лавке/инвентаре** масштабируются с уровнем (продажа слабее). Полный антиинфляционный тюнинг — дальше.

### 2. Инвентарь и Лут
- [x] UI инвентаря и экипировки (оружие, броня, аксессуар).
- [x] Механика использования расходников (например, зелье отката стрика).
- [x] Пассивные бонусы от экипировки.
- [x] Система RNG-дропа после квеста.
- [x] Визуальная полировка (цвета редкости).

### 3. Древо Навыков
- [x] Ветки (Ассасин, Маг, Танк), карточки навыков.
- [x] Изучение за SP, требования к уровню.
- [x] Механика «Медитации» (фокус-таймер).

---

## 🤖 Фаза 3: Интеграция «Системы» (Умный ИИ-Агент)
*Оживление приложения. ИИ как балансировщик и Гейм-мастер.*

### 1. Подключение и Чат
- [x] `AIService` и чат с Системой (`SystemChatPage`).
- [x] Подмешивание `agent_rules.txt` для формального тона.

### 2. Интеллектуальная Генерация и Античит
- [x] Запрос квестов по самым низким статам, генерация `urgent` квестов.
- [x] **ИИ-Балансировщик (Античит):** При сохранении ручного квеста с API-ключом ИИ пересчитывает награды/сложность/обязательность (JSON + клампы по уровню); без ключа остаются ползунки.

### 3. Адаптация и События
- [x] Отслеживание тегов и выдача скрытых классов (напр. «Кодер»).
- [~] **Поведенческий анализ:** Три провала квестов с **сложностью ≥ 4** подряд → автоматический мягкий квест `morale_recovery` (без ИИ). ИИ-анализ текстов провалов — дальше.
- [~] **События и Аномалии:** **«Кровавая луна»** — случайный старт при холодном старте (~5%, 24 ч): двойные штрафы за провал, бонус к броску дропа; баннер на профиле. Другие ивенты — дальше.

---

## 📜 Фаза 4: Погружение, UX и Нарратив
*Удержание пользователя, обучение и интеграция в ОС.*

### 1. Онбординг и Сюжет
- [x] **Пробуждение (Tutorial):** Три квеста «Пробуждение» в ленте при первом старте (Hive-флаг + тег `awakening`) + интерактивная сцена (showDialog) один раз.
- [~] **Главный Сюжет (`story` квесты):** Вехи на **10 / 25 / 50** уровнях (`story_gate_*`, `DatabaseService.trySpawnStoryMilestones`). Привязка фич: «Режим Монарха» включается после выполнения `story_gate_50` — дальше расширяем на другие фичи.
- [~] **Эндгейм:** Базовый «Режим Монарха» активируется с 50 уровня (баннер в профиле + достижение). Отдельные эндгейм-механики и контент — дальше.

### 2. Уведомления и Виджеты
- [x] **Локальные уведомления (`flutter_local_notifications`):** За 2 ч до `expiresAt` у активных квестов с дедлайном (до 12 ближайших; канал Android + `POST_NOTIFICATIONS`). Пересборка при каждом обновлении списка квестов.
- [x] **Виджеты рабочего стола (`home_widget`):** Виджет «Статус Охотника» (статус охотника + мини-список ежедневных квестов), авто‑обновление при изменениях.

### 3. Адаптивный UI и Темы
- [x] Движок тем (`theme_skin_id`): Скины Solo Leveling, Мурим, Архимаг.
- [x] Кастомные названия статов.
- [x] **Доступ к настройкам с любой вкладки:** глобальная кнопка-шестерёнка в `HomeShell` (верх‑право), без дублей.
- [~] Продвинутые анимации: Интеграция Rive/Lottie для экранов Level Up и открытия легендарного дропа.

---

## 🌐 Фаза 5: Социальный Слой и Облако
*Синхронизация, мультиплеер и соревновательный элемент.*

### 1. Cloud Sync (Облако)
- [x] Интеграция Supabase (`CloudSyncAdapter`).
- [x] **Аккаунт и облако (UX):** экраны `Аккаунт` и `Облачная синхронизация` разделены; понятные статусы (локальный режим / нужен вход / активно).
- [x] **Google OAuth:** кнопка «Войти через Google» через `Supabase.auth.signInWithOAuth` + deep link callback в AndroidManifest.
- [x] **Разрешение конфликтов (документация):** Стратегия LWW / слияние по `id` зафиксирована в комментарии к `CloudSyncAdapter`; реализация по слоям — по мере доработки `pullRemote`.

### 2. Асинхронный Мультиплеер
- [x] Гильдии (локальный фундамент): имя с **уровня 15** (`ProgressionGates.guildMinLevel`), уже сохранённое имя доступно раньше (миграция).
- [x] **Гильдия вынесена в отдельный хаб:** отдельная вкладка/экран `GuildHubScreen` (не в Settings), чтобы социалка была “в центре” и готова под Supabase.
- [ ] **Таблицы лидеров:** Глобальные или локальные (среди друзей) рейтинги по уровню и выполненным квестам.
- [ ] **Профили друзей:** Возможность добавить «Охотника» по ID и смотреть его статы/экипировку.
- [ ] **Мировые Врата (PvE-ивенты):** Общие цели для сообщества (напр. «Сделать 10 000 отжиманий суммарно за выходные»). Награда выдается всем участникам рейда.
- [ ] **Кровные контракты (Co-op accountability):** парные квесты с взаимной ответственностью (если партнёр проваливает — применяется мягкий штраф/эффект для связанного прогресса) + совместные уведомления.
- [x] Достижения и титулы (выводятся в глобальный профиль).

---

## 🪐 Фаза 6: «Мультивселенная систем» (смена философии без ломки ядра)
*Меняем смысл, терминологию, тон “голоса” и правила — не переписывая базовую механику сразу.*

### 1) Философия как надстройка (SystemConfig)
- [x] `system_id` (solo/mage/cultivator/custom) хранится в Hive (`DatabaseService`).
- [x] `SystemConfig` + `SystemDictionary` + каталог систем (`core/systems/*`).
- [x] Riverpod: `activeSystemProvider` + `activeSystemIdProvider`.
- [x] Подключение словаря в UI (первые ключевые места): профиль (опыт), квесты (награды), магазин (валюта), навыки (лейбл уровня), статистика в настройках.
- [x] Бэкап/восстановление: `activeSystemId` сохраняется в `exportGameBackupJson`/`importGameBackupJson`.

### 2) UI выбора философии
- [x] Экран `SystemSelectionScreen`: карусель (PageView) 4 систем с карточками, терминами и CTA «Применить».
- [x] Доступ из настроек: tile «Философия» открывает карусель.
- [x] One-time автопоказ после создания охотника: Hive-флаг `system_selection_shown` (+ включён в backup).

### 3) Правила философий (SystemRules)
- [x] Каркас `SystemRules` + `SoloRules` + провайдер `activeSystemRulesProvider` (пока без изменения математики).
- [x] Подключить правила в точки принятия решений: награды/штрафы/фокус-сессия/приоритет типов квестов.

### 4) Реальные отличия геймплея (Mage / Cultivator)
- [x] Mage: руны (теги), комбинации → баффы/достижения; энтропия (затухание) по дням.
- [x] Cultivator: “закрытая медитация” на базе focus-таймера; “небесные испытания” как квесты на прорыв.

### 5) Custom System (безопасный конструктор)
- [x] UI выбора терминов (энергия/уровень/валюта/статы) + выбор шаблона правил (2–3 пресета).
- [x] Затем — пользовательский промпт ИИ (с ограничениями/валидацией).

---

## 🌌 Фаза 7: «Мультивселенная 2.0» — раздельные сейвы, онбординг-путешествие и AI-конструктор тем
*Делаем AAA-ощущение: отдельный прогресс на каждую философию, правильный старт и безопасное создание кастомных “миров”.*

### 1) Раздельный прогресс (Multiverse Saves)
- [x] **Модель сейвов по системам:** `Hunter` хранится отдельно по `system_id` (ключи `profile_<id>`) + legacy-фолбэк для `solo`.
- [x] **Квесты и инвентарь по системам:** квесты неймспейсируются тегом `system_id_*` и фильтруются по активной системе; meta-прогресс (`tagCounts`/`achievements`/records) разнесён по системам.
- [x] **Миграция:** старый единый сейв распознаётся как `solo`, новые профили создаются при первом входе в систему.
- [~] **Облако:** бэкап теперь экспортирует `hunterProfiles` и все квесты с сохранением `system_id_*`; но пока не полностью разнесены все мета-метрики прогресса по системам.

### 2) Идеальный поток запуска (Onboarding Journey как State Machine)
- [~] **State Machine запуска:** холодный старт уже ведёт через `SystemSelectionScreen`, а диалог “Пробуждение” больше не может стартовать до завершения выбора философии.
- [x] **Опциональный лор:** отдельная кнопка/раздел “Лор” (не заставлять всех читать).
- [x] **Туториал по философии:** 3 шага “Пробуждения” уже есть и теперь корректно привязаны к активной системе (через system-scoped флаги + корректное семплирование при первом входе).

### 3) Глубокие темы (не только `ThemeData`)
- [~] **`ThemeExtension` для RPG-токенов:** запущено на токенах редкости (цвет/свечение) через `RpgThemeTokens`.
 - [x] **Две “витринные” темы:** Маг (archmage) и Культиватор (cultivation) переключаются при смене `system_id` (через `theme_skin_id`), а редкости адаптируются под них.
 - [x] **Совместимость с дизайн-системой:** ключевые promo/UI элементы теперь используют `Theme.of(context).colorScheme` и `ThemeExtension` (RpgThemeTokens), а не захардкоженные неоновые цвета.
- [x] **Радикальная смена атмосферы (SystemVisuals):** `ThemeExtension` уровня “мира” + Smart Background:
  - [x] ассеты фона (SVG в `assets/backgrounds/*` + поддержка URL/file + fallback)
  - [x] эффекты частиц (sparkles/runes/petals)
  - [x] формы/бордеры (radius токен + применение в `ProfileNeonCard`)
  - [~] типографика по системе (Manrope остаётся базой; можно добавить display-font позже)

### 4) AI-конструктор тем/систем (Structured Outputs / JSON Schema)
- [x] **JSON-схема “системы”:** реализована через `GeneratedSystemThemeJson` (терминология + цвета + правила).
- [x] **Генерация через выбранный AI-провайдер:** добавлен UI “Лаборатория творения” в `CustomSystemBuilderPage` с preview и кнопкой “Инициализировать”.
- [~] **Валидация и безопасность:** safe-parsing (JSON extraction) + типовая валидация/парсинг hex цветов; preview/rollback и лимиты будут дальше.
- [x] **Хранение множества Custom-систем:** поддержка `custom_<slug>` и каталога запущена (локальная запись + включено в `exportGameBackupJson`/`importGameBackupJson`).

---

## 🎬 Фаза 7.5: Cinematic Onboarding (кинематографичное погружение)
*Переводим старт в “игровую сцену”: лор → мастер → персонализация → генерация стартовых веток.*

### 1) Портал Лора (The Lore Portal)
- [ ] **Полноэкранный лор-экран:** `Stack` + арт системы (Solo/Mage/Cultivator/Custom) с затемнением внизу.
- [ ] **Эффекты атмосферы:** Parallax (лёгкий), частицы (Lottie/Rive) по системе: цифровой дождь / руны / туман / лепестки.
- [ ] **Typewriter-текст:** постепенное появление описания мира и правил.
- [ ] **CTA:** “Шагнуть в неизвестность / Принять зов” → переход к мастеру.

### 2) Встреча с Мастером (The Encounter)
- [ ] **Персонаж-мастер:** арт + характер и реплики зависят от философии.
- [ ] **Диалог вместо формы:** сценарные реплики + короткие ответы:
  - [ ] имя (роль игрока)
  - [ ] увлечения (теги/чипы: код/спорт/искусство/учёба/бизнес/здоровье…)
  - [ ] цель (“что хочешь подтянуть”)
- [ ] **UX:** плавный переход (растворение/вспышка) между лором и мастером.

### 3) AI Engine (Supabase Edge Function) — “инициализация души”
- [ ] **Edge Function:** принять JSON профиля онбординга + `system_id` + термины/тон.
- [ ] **Строгий JSON-ответ (schema):**
  - [ ] `hidden_class` (скрытый класс) + причины/теги
  - [ ] стартовая ветка навыков (первый “путь”)
  - [ ] стартовая цепочка квестов 3–5 (ветвление/набор “первых побед”)
  - [ ] (опц.) стартовые титулы/баффы/предупреждения
- [ ] **Лоадер-сцена:** “Синхронизация Души / Калибровка Системы” (не блокировать навсегда: таймаут + fallback).

### 4) Пробуждение (The Awakening)
- [ ] **Финальное напутствие мастера** (в стиле системы) + “вспышка” смены атмосферы UI.
- [ ] **Мгновенная выдача результата:** в ленте квестов уже лежат AI-сгенерированные ветки/цепочки.
- [ ] **Сохранение:** ответы пользователя + результаты AI в локальную БД и (при наличии аккаунта) в облако.

---

## 🌟 Фаза 8: Титулы, Достижения, AI-операторы (Edge Functions) и удержание
*Переводим “умный ИИ” в безопасные серверные операторы, добавляем пассивные эффекты от титулов и триггеры retention.*

### 1) Титулы и пассивные баффы (Equippable Titles)
- [ ] Добавить поле `equippedTitle` (и каталог `TitleDef`) в прогресс охотника.
- [ ] Связать условия выдачи титулов с событиями: стрики, типы квестов, открытие скрытого класса, `system_event`.
- [ ] Реализовать UI: карточка титула на профиле + экран “Титулы/Достижения”.
- [ ] Дать эффект через существующую систему `Buff` (effectId/значение/экспирация).

### 2) AI-Перехватчик ручных квестов (anti-cheat + авто-теги)
- [ ] При создании ручного квеста отправлять текст в Supabase Edge Function.
- [ ] Edge Function возвращает строгое JSON-описание: `category`, `tags`, `difficulty_rank`, `suggested_exp`, `suggested_gold`, `mandatory?`.
- [ ] Приложение применяет результат как подсказки/предзаполнение формы квеста (с подтверждением пользователем).
- [ ] Встроить `system_event` (например, “склонность к алгоритмам”) как источник для скрытых классов/титулов.

### 3) Наблюдатель паттернов и “скрытая эволюция”
- [ ] Собирать сигналы: частота тегов, длительность стрика, типы квестов, участие в penalize/bonus событиях.
- [ ] Генерировать `system_event` (unlock hidden class / title / уведомление) на сервере.
- [ ] Реализовать UI-реакцию: fullscreen “получено” + запись в достижения.

### 4) Edge Functions как источник истины (без ключей в клиенте)
- [ ] Создать Edge Function(ы): `analyze_quest`, `detect_patterns` и (позже) `generate_quest_chain`.
- [ ] Переключить формирование prompts/терминологии по активной философии (system prompt + terminology).
- [ ] Добавить строгую схему вход/выход и валидацию (safe parsing, лимиты, дефолты, откат).

### 6) MMO-слой (архитектурные заготовки для социалки/рейдов/поиска)
- [ ] **Backend (Supabase):** таблица `public_profiles` (минимум PII): `display_name`, `active_system_id`, `guild_id`, `current_level`, `rank`, `updated_at`.
- [ ] **SocialService:** поиск по ID, профиль друга, запросы/инвайты, “зал славы” (global search).
- [ ] **Ники формата `Nick#1234`:** генерация/проверка, уникальность, поиск по нику или полному ID.
- [ ] **Иконка философии в социалке:** рядом с ником показывать значок активной системы (кристалл/меч/инь‑ян) → эффект “мультивселенной”.

### 7) Конфликты синхронизации и оффлайн-прогресс (Race Conditions)
- [ ] **Стратегия:** профиль = LWW, активности = append-only (журнал событий).
- [ ] **Арбитраж на сервере:** Edge Function валидирует оффлайн-прогресс при возвращении в сеть (конфликт гильдии/системы/устройства).

### 8) Экономический баланс “Мультивселенной”
- [ ] **Единый коэффициент ценности:** внутренние `ValuePoints` одинаковы для всех, а на UI отображаем философскую валюту (золото/мана/ци…).
- [ ] **Анти-эксплойт:** запрет “фарма через лёгкую философию” для глобальных рейтингов — нормализация/коэффициенты.

### 5) Полировка UX (после архитектурного шага)
- [ ] Haptic Feedback на ключевых действиях: выполнение квеста, левелап, покупка, unlock.
- [ ] Звуки UI-ивентов (опционально).
- [ ] Staggered animations для списков квестов/достижений.

---

## 🧿 Фаза 9: Системы удержания (Retention) и “забота о пользователе”
*Механики, которые уменьшают выгорание и увеличивают “возвраты” на долгом горизонте.*

### 0) Навигация и цикл “приключений” (Dungeons-first)
- [ ] **Динамические labels навигации:** нижняя панель читает названия из `activeSystemProvider` (Solo/Mage/Cultivator разные слова).
- [ ] **Рокировка “Лавка vs Активности”:**
  - [ ] Магазин уходит “вверх” (иконка в AppBar/верх‑право на ключевых экранах).
  - [ ] Внизу появляется вкладка “Активности/Подземелья/События” как центр retention.

### 0.1) Подземелья (Dungeons) как цепочки, а не одиночные квесты
- [ ] **Модель Dungeon:** цепочка этапов (квестов) + состояние прохождения.
- [ ] **Fail-logic:** провал этапа = вылет из данжа (хардкор Solo Leveling-ощущение).
- [ ] **Примеры:** “Неделя без сахара” (7 этапов), “Лес знаний” (читать → конспект → практика).

### 0.2) Рейды (Guild Goals)
- [ ] **Совместные цели гильдии:** суммарные метрики (например, 500 часов фокуса).
- [ ] **Награды гильдии:** Guild XP, уровень гильдии, общие бонусы (+% золота/ValuePoints).

### 0.3) Лидерборды (топливо азарта)
- [ ] **Индивидуальные:** ранг E→S, уровень, кол-во сюжетных квестов, стрики.
- [ ] **Гильдейские:** Guild XP/дневной вклад, уровень гильдии, сезонные таблицы.

### 1) Регрессия (Roguelite-элемент)
- [ ] Добавить кнопку “Активировать Регрессию (Сброс)” на заданных порогах.
- [ ] Ввести валюту “Фрагменты Кармы/Осколки Души” и глобальный магазин перков (во всех философиях).

### 2) Святилище (пауза без потери прогресса)
- [ ] Предмет “Билет в Святилище”: пауза на 1–3 дня без штрафов и с сохранением стрика.

### 3) Интеграция с физическим сосудом (Health)
- [ ] Apple Health / Google Fit: шаги/сон/пульс как источники событий для бонусов/мягких дебаффов.

### 4) Кровные контракты (Co-op accountability)
- [ ] Связь аккаунтов через Supabase и парные квесты с взаимной ответственностью.
- [ ] Общие уведомления + синхронизация статуса выполнения.

---

## 🚀 Фаза 10: Product Release Readiness (2026) — “строгий PM + Tech Lead”
*Закрываем последние дырки до выхода: offline fallback, progressive disclosure, async/perf AI, wearable-экосистема и product-описание.*

### 1) Offline Fallback (эвристика без ИИ при отсутствии сети)
- [ ] Добавить локальный “глупый” категоризатор квестов (эвристики по ключевым словам/тегам/длине) чтобы выдавать базовые награды без Edge Function/LLM.
- [ ] При появлении сети: ретро-отправка “оценки” на сервер и (опционально) апгрейд наград/скрытого достижения, без потери целостности сейва.
- [ ] Явный UX: показать пользователю, что “система обновит награду позже”, чтобы не было ощущения лагов/ошибок.

### 2) Progressive Disclosure (уменьшаем когнитивную перегрузку)
- [ ] Ввести gating по уровню/стадиям онбординга: до 5 уровня скрывать часть разделов (например, инвентарь/навыки) или показывать только “скелет” с объяснением.
- [ ] Добавить сюжетные/системные подсказки при открытии новых экранов: “Ваш сосуд окреп. Открыт доступ к …”.
- [ ] Свести количество параллельных сущностей (EXP/Gold/SP/баффы/экипировка) на стартовых экранах до минимума.

### 3) Производительность AI (скорость добавления задач)
- [ ] Разделить “создание квеста” и “AI-оценку”: квест создаётся мгновенно, AI-модуль догоняет через асинхронный шаг.
- [ ] Ограничить latency: таймауты, быстрые модели для категоризации, fallback к локальным эвристикам.
- [ ] Не блокировать UI: все операции должны быть non-blocking (loading/error) без повторного создания квестов.

### 4) Wearables Ecosystem (Apple Watch / Wear OS / фитнес-браслеты)
- [ ] Определить MVP-интеграции (минимум: шаги/сон/активность) и выбрать приоритет платформ.
- [ ] Добавить “контракт событий” в архитектуру (provider -> domain event -> система) так, чтобы расширять позже.

### 5) Product-описание (README Killer Features)
- [ ] Обновить `README.md` блоком “Killer Features” (Multiverse Engine, AI Game Master, Smart Anti-Cheat, Hidden Evolution, Penalty Zone, Offline-first + Cloud Sync).
- [ ] Добавить раздел “What’s next” и “How to run” (ключевые `--dart-define` для Supabase/AI).

## 📁 Связанные файлы в репозитории

| Область | Путь |
|--------|------|
| Дорожная карта | `plan.md` |
| Провайдеры и логика | `lib/src/services/providers.dart` |
| Модель охотника | `lib/src/models/hunter_model.dart` |
| Ранг E–S и ключи титулов (UI) | `lib/src/core/hunter_display.dart` |
| Экран профиля | `lib/src/features/hunter/hunter_profile_page.dart` |
| ИИ HTTP и промпты | `lib/src/services/ai_service.dart` |
| Кривая опыта | `lib/src/core/experience_curve.dart` |
| Логика дропа (RNG) | `lib/src/core/loot_drop_logic.dart` |
| Эндгейм (Монарх) | `lib/src/core/monarch_mode.dart` |
| Данные теплокарты | `lib/src/core/daily_quest_heatmap.dart` |
| Аналитика профиля | `lib/src/features/hunter/widgets/profile_analytics_section.dart` |
| Квесты и БД | `lib/src/models/quest_model.dart`, `lib/src/services/database_service.dart` |
| Экран квестов | `lib/src/features/quests/quests_page.dart` |
| Правила агента | `assets/agent_rules.txt` |
| Облако (Supabase) | `lib/src/services/cloud_sync/cloud_sync_adapter.dart` |
| Уведомления квестов | `lib/src/services/quest_notification_service.dart` |
| Масштаб золота / цен лавки и продажи | `lib/src/core/economy_scale.dart` |
| Пороги уровня (гильдия и т.д.) | `lib/src/core/progression_gates.dart` |
| Сиды онбординга / сюжетных вех | `lib/src/core/tutorial_quests_seed.dart`, `lib/src/core/story_milestone_seed.dart` |
| Темизация и скины | `lib/src/core/theme.dart`, `assets/themes/*.json` |
| Пресеты предметов / навыков | `lib/src/data/items_data.dart`, `lib/src/data/skills_data.dart` |
| Редкость предметов (UI) | `lib/src/core/item_rarity_style.dart` |
| Фокус-оверлей | `lib/src/features/focus/focus_session_layer.dart` |
| UI облака / аккаунта | `lib/src/features/settings/account_page.dart`, `lib/src/features/settings/cloud_sync_page.dart` |
| Выбор философии | `lib/src/features/system/system_selection_screen.dart` |
| Конструктор Custom System | `lib/src/features/system/custom_system_builder_page.dart` |
| Системы (философии) | `lib/src/core/systems/*` |
| Старый диалог облака (legacy) | `lib/src/features/settings/widgets/cloud_sync_dialog.dart` |

После крупных фич обновляйте чеклисты в этом файле.

---

## 💭 Заметки, идеи и открытые вопросы

*Сюда можно добавлять размышления при ревью плана — не часть жёсткой спецификации, а рабочие гипотезы.*

1. **Приоритизация:** Фазы 0 (безопасность ключей, фон, тесты) и Фаза 4 (пуши) сильно влияют на доверие и удержание, но не «видны» в демо. Имеет смысл явно выделить *релизные вехи* (например, «MVP+» = ключи + минимальные тесты опыта/квеста).

2. **ИИ-балансировщик vs ручная форма:** Сейчас игрок задаёт награды вручную. Переход к «только название + суть» потребует жёсткой валидации JSON от модели, откатов при ошибке и, возможно, **потолков** по уровню охотника — иначе античит останется обходимым.

3. **Штрафная зона и дебаффы:** Нужна единая модель баффов/дебаффов в Hive (тип, источник, `expiresAt`), иначе красный штрафной квест будет «особым случаем» в коде квестов.

4. **Конфликты облака:** LWW проще, но для RPG с квестами лучше документировать **правила слияния** (например: квесты по `id`, охотник по `updatedAt`, инвентарь — осторожно с дублями).

5. **Социальный слой:** Таблицы лидеров и друзья потребуют бэкенда шире, чем текущий бэкап (`game_backups`). Стоит заранее решить: расширять Supabase-схему или отдельный сервис.

6. **Нарратив:** Сюжетные `story`-квесты на уровнях хорошо стыкуются с уже заведённым типом квеста в модели — остаётся контент, триггеры разблокировки и UI «вехи», чтобы не было просто ещё одной строки в списке.

7. **Профиль и «AAA»-UX:** Ранг E–S, титулы, компактный опыт и неоновые карточки уже задают тон; дальше радар/heatmap (Фаза 1) логично reuse те же токены темы и шрифты цифр, чтобы не плодить второй визуальный язык.

Если появятся новые вопросы по приоритетам или формулировкам чеклиста — дописывайте прямо в этом разделе или правьте фазы выше.
